// src/components/svg/PlanetOrbit.tsx
// Elliptical orbit animation for the loading screen.
// Planet rotates 360° around center, 2s per revolution, infinite.
// Full implementation in Sprint D.
// Colors: accentViolet from theme.ts — never inline.

import { View } from 'react-native';

export function PlanetOrbit() {
  // Sprint D: SVG ellipse path + Animated.loop rotating planet glyph
  return <View className="w-32 h-32 items-center justify-center" />;
}
